const {define} = require("cypress");
    module.exports = defineConfig({
        e2e:
    
    setupNodeEvents(on, config)
        //implement node event listerners here

    },
    {chromeWebSecurity: false});
